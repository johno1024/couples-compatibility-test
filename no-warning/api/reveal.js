const {getStore}=require('./_store');
module.exports=async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const revealToken=String(req.query?.revealToken||'');const s=[...getStore().values()].find(x=>x.revealToken===revealToken);if(!s)return res.status(404).json({error:'Invalid or expired Reveal Link.'});if(!s.player1.locked||!s.player2.locked)return res.status(200).json({ready:false});return res.status(200).json({ready:true,player1:{name:s.player1.name,answers:s.player1.answers},player2:{name:s.player2.name,answers:s.player2.answers}});};
