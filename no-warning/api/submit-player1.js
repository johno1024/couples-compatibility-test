const {getStore}=require('./_store');
module.exports=async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const {sessionId,token,name,answers}=req.body||{};const s=getStore().get(sessionId);if(!s)return res.status(404).json({error:'Missing or expired session.'});if(token!==s.player1Token)return res.status(403).json({error:'Invalid Player 1 token.'});if(s.player1.locked)return res.status(409).json({error:'This test is already completed.'});if(!answers||Object.keys(answers).length!==40)return res.status(400).json({error:'All 40 questions must be answered.'});s.player1={name:String(name||s.player1.name).trim().slice(0,32),answers,locked:true};getStore().set(s.id,s);return res.status(200).json({ok:true});};
