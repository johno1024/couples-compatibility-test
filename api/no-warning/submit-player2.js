const {read,put}=require('./_store');
module.exports=async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const {shareToken,name,answers}=req.body||{};const s=await read('share',shareToken);if(!s)return res.status(404).json({error:'Invalid or expired Share Link.'});if(!s.player1.locked)return res.status(409).json({error:'Player 1 has not locked their answers yet.'});if(s.player2.locked)return res.status(409).json({error:'This test has already been completed.'});if(!answers||Object.keys(answers).length!==40)return res.status(400).json({error:'All 40 questions must be answered.'});const player2Name=String(name||'').trim().slice(0,32);if(!player2Name)return res.status(400).json({error:'Enter a name or nickname.'});s.player2={name:player2Name,answers,locked:true};await put(s);return res.status(200).json({ok:true,revealToken:s.revealToken});}catch(e){return res.status(503).json({error:'Session storage is temporarily unavailable.'})}};
